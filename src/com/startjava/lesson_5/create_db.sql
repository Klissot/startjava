CREATE TABLE Jaeger (
    id  SERIAL PRIMARY KEY,
    modelName   TEXT,
    mark        INTEGER,
    height      REAL,
    weight      INTEGER,
    status      TEXT,
    origin      TEXT,
    launch      INTEGER,
    kaijuKill   INTEGER
);